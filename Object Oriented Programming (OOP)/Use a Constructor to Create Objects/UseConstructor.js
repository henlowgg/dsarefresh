function Dog() {
    this.name = "Rupert";
    this.color = "brown";
    this.numLegs = 4;
  }
  // Only change code below this line

  let hound = new Dog();

  // just assigning the new object 'hound' to Dog to create a new instance of 'Dog'

  // important to note that 'this' inside the constructor always refers to the object being created

  