function repeatStringNumTimes(str, num) {
    let repeatStr = "";
    for (let i = 0; i < num; i++) {
        repeatStr += str;
    }
    
    return repeatStr;
  }
  
  repeatStringNumTimes("abc", 3);


  // need to write a for loop 

  // also need to declare a variable for the new array to be stored in 

