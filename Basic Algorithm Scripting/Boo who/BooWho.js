function booWho(bool) {
    return typeof bool === "boolean";
  }
  
  booWho(null);


// just use typeof method


// typeof bool === "boolean";

// will return true or false against all test cases as it strictly checks if it is a boolean
