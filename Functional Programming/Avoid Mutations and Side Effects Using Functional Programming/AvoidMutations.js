// The global variable
let fixedValue = 4;

function incrementer() {
  // Only change code below this line

  return fixedValue + 1;

  // Only change code above this line
}

// initially wanted to use a for loop out of habit, but you only need to return the variable

// fixedValue and increment (+) by 1 (1)

// return fixedValue + 1;

// using the ++ operator on fixedValue will mutatue fixedValue, meaning it will no longer reference the same value it was assigned with