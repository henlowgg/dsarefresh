// Only change code below this line
const increment = (number, value = 1) => number + value;
// Only change code above this line


// multiple declarations can be presented within the first set of clappers ()

//  however it can be done like (a, b = 1, c = 2 + 4)