let myString = "Hello, World!";
let myRegex = /Hello/;
let result = myRegex.test(myString); // Change this line

// just add .test(myString) to the end of myRegex

//ok i see, so its bascially just testing myString to see if it has the word Hello in it, i.e., myRegex.test(myString) tests if hello is in myString