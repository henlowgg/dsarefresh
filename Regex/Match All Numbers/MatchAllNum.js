let movieName = "2001: A Space Odyssey";
let numRegex = /\d/g; // Change this line
let result = movieName.match(numRegex).length;

// so just lowercase d with a forward slash

// theres a pattern for shortcuts to just use forward slash before whatever letter

// looks like this /\d/g